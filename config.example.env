# Configuration d'exemple pour NinjaLinking SaaS
# Copiez ce fichier vers server/.env et client/.env.local

# ===========================================
# CONFIGURATION SERVEUR (server/.env)
# ===========================================

# Port du serveur backend
PORT=5000

# Environnement (development/production)
NODE_ENV=development

# Clé secrète JWT (changez en production!)
JWT_SECRET=ninjalinking-secret-key-2024

# Base de données
# SQLite (développement)
DATABASE_URL=sqlite:./database.sqlite

# PostgreSQL (production)
# DATABASE_URL=postgresql://username:password@localhost:5432/ninjalinking_db

# Configuration CORS
CORS_ORIGIN=http://localhost:3000

# Limitation de débit
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# CONFIGURATION CLIENT (client/.env.local)
# ===========================================

# URL de l'API backend
NEXT_PUBLIC_API_URL=http://localhost:5000

# Configuration Next.js
NEXT_PUBLIC_APP_NAME=NinjaLinking
NEXT_PUBLIC_APP_VERSION=1.0.0

# ===========================================
# CONFIGURATION PRODUCTION
# ===========================================

# Pour la production, changez ces valeurs:
# - JWT_SECRET: utilisez une clé forte et unique
# - NODE_ENV=production
# - DATABASE_URL: utilisez PostgreSQL
# - CORS_ORIGIN: votre domaine de production
# - NEXT_PUBLIC_API_URL: votre domaine de production

